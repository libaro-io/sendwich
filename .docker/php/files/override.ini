; Local PHP overrides (mounted to /usr/local/etc/php/conf.d/local.ini)

; Allow uploading receipt photos taken on a phone (a few MB each).
upload_max_filesize = 12M
post_max_size = 13M

; Headroom for image handling.
memory_limit = 256M
